# خطة تصميم واجهة Akwam Player

## الرؤية

تطبيق عربي RTL للهواتف portrait يقدّم تجربة اكتشاف ومشاهدة بسيطة ومريحة بيد واحدة، مع هوية داكنة سينمائية مستوحاة من موقع ak.sv دون نسخ واجهته حرفيًا. التطبيق لا يستخدم سيرفرًا خاصًا؛ يطلب البيانات مباشرة من المصدر العام عند توفرها، ويعرض حالات التحميل والخطأ بوضوح.

## الشاشات

| الشاشة | المحتوى والوظيفة |
|---|---|
| الرئيسية | ترحيب مختصر، شريط بحث بارز، اختصارات الأفلام والمسلسلات والتلفزيون والمنوعات، وقسم أحدث الإضافات ببطاقات أفقية. |
| نتائج البحث | عنوان البحث، عدد النتائج إن توفر، شبكة بطاقات مرنة، وحالة فارغة مع اقتراحات بديلة. |
| الأحدث | قائمة أو شبكة للمحتوى المضاف حديثًا مع شارات السنة والجودة والتقييم. |
| تفاصيل المحتوى | صورة الغلاف، العنوان، التقييم، النوع، اللغة، الترجمة، السنة، المدة، القصة، لقطات، وزرا المشاهدة والتحميل/فتح الرابط. |
| المشاهدة | مشغل فيديو عند توفر رابط قابل للتشغيل، أو فتح آمن داخل متصفح النظام/المتصفح داخل التطبيق عند تعذر التشغيل الأصلي. |
| المفضلة محليًا | عناصر يحفظها المستخدم على الجهاز عبر AsyncStorage فقط، دون حساب أو مزامنة. |
| الإعدادات | اختيار المظهر، مسح المفضلة، فتح المصدر، معلومات التطبيق، وتنبيه يوضح أن التطبيق لا يستضيف المحتوى. |

## تدفقات الاستخدام الرئيسية

1. يفتح المستخدم التطبيق، يكتب اسم فيلم أو مسلسل في شريط البحث، يضغط «بحث»، ثم يختار بطاقة من النتائج.
2. ينتقل المستخدم إلى صفحة التفاصيل، يراجع البيانات، ثم يضغط «مشاهدة». إذا توفر رابط مباشر متوافق يعرض التطبيق المشغل، وإلا يفتح الرابط عبر المتصفح مع رسالة واضحة.
3. يضغط المستخدم على رمز القلب في البطاقة أو التفاصيل، فيُحفظ العنصر محليًا ويظهر في شاشة المفضلة.
4. عند فشل الشبكة، يحتفظ التطبيق بالواجهة والمفضلة المحلية ويعرض رسالة قابلة لإعادة المحاولة بدل شاشة فارغة.

## الهوية البصرية

الألوان الأساسية: خلفية ليلية `#0B0D12`، سطح البطاقات `#151923`، سطح مرتفع `#1D2330`، نص أساسي `#F7F8FA`، نص ثانوي `#9BA4B5`، لون العلامة البنفسجي السينمائي `#8B5CF6`، ولون الإجراء السماوي `#22D3EE`. تستخدم الأزرار الأساسية تدرجًا بصريًا من البنفسجي إلى السماوي مع تباين مرتفع.

الخطوط تكون كبيرة وواضحة، والعناوين قصيرة، مع مساحات لمس لا تقل عن 44 نقطة تقريبًا. تستخدم البطاقات زوايا 18–22 نقطة، وصورًا بنسبة ملصق سينمائي، وأيقونات Material Icons متسقة. لا تُستخدم حركات مبالغ فيها؛ تقتصر الحركة على انتقالات خفيفة وحالة ضغط واضحة.

## قرارات تجربة المستخدم

يظهر شريط البحث في أعلى الرئيسية دائمًا تقريبًا، وتوضع الإجراءات الأساسية في الثلث السفلي أو بمحاذاة الإبهام. لا يُطلب تسجيل دخول، ولا تُضاف حسابات أو مزامنة أو خادم لأن ذلك خارج النطاق الحالي. كل رابط خارجي يوضح للمستخدم إلى أين سينتقل، وتظهر حالات loading وerror وempty بالعربية.
